Development Boomber game with AI-Reinforment

git clone "link"

touch <file> // create to file

git add <file>

git status

git commit -m "desciption"

git log

git push

git commit -a "description" == 'git add <file> -> git commit -m "description" ' // with file existed

vim <file> //open and modify

Esc + :wq // save file -> teminal -> :wq save and quit

git diff // View file changed
